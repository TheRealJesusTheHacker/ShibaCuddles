# ShibaCuddles - runtime dependencies only.
# Dev/test tooling lives in requirements-dev.txt.

# Core: packet crafting for ARP/ICMP discovery (imported lazily at runtime)
scapy>=2.5.0

# GUI dashboard
PyQt6>=6.0.0
PyQt6-Charts>=6.0.0

# WiFi testing helpers (install separately via your OS package manager):
#   Ubuntu/Debian: sudo apt-get install aircrack-ng
#   Fedora:        sudo dnf install aircrack-ng
#   macOS:         brew install aircrack-ng
